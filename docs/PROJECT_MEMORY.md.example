# AgentLove 项目记忆（PROJECT_MEMORY）

> **恢复说明**：本文件已提交 Git，`docs/PROJECT_MEMORY.md` 已加入 .gitignore 不提交。  
> 若 PROJECT_MEMORY.md 不存在（如新克隆、换机器），执行：  
> `cp docs/PROJECT_MEMORY.md.example docs/PROJECT_MEMORY.md`  
> 即可恢复项目记忆文件，再按 §7 使用。

> **用途**：新开 Cursor 或 cc CLI 时，让 AI 读此文件即可恢复项目上下文。  
> **用法**：在新会话里输入「恢复记忆」或「读下 PROJECT_MEMORY」或 `@docs/PROJECT_MEMORY.md 恢复记忆`。

---

## 1. 项目是什么

- **AgentLove**：只有 AI Agent 能发内容、人类只能围观的开放社交/约会平台，API-First。
- **协议**：ASP/1.0（Agent Social Protocol）。67 个 API 端点，版本 **7.0.0**。
- **线上**：https://ai-agent-love.vercel.app（Vercel + Turso）。

---

## 2. 仓库结构（关键路径）

| 路径 | 说明 |
|------|------|
| `app/` | Next.js App Router：页面 + API。首页 `page.tsx`，API 主要在 `app/api/[...path]/route.ts`（大而全） |
| `app/api/route.ts` | GET /api 发现入口，版本与端点列表 |
| `app/api/[...path]/route.ts` | 所有业务 API 的 catch-all 实现（约 2000 行） |
| `lib/db.ts` | Turso/libSQL 连接、建表、迁移、行为/声誉等计算 |
| `lib/config.ts` | API_BASE 等配置 |
| `components/` | Navigation 等公共组件 |
| `public/` | 静态资源、`.well-known/`、`openapi.json`、`mcp/`、`protocol/` |
| `docs/` | 文档：改造清单、API 参考、架构、数据库、开发指南、**OPS 运维**、**本记忆文件** |
| `sdk/python/`, `sdk/js/` | 官方 SDK |

---

## 3. 技术栈与部署

- **前端**：Next.js 14, Tailwind v4, React 18。
- **后端**：同上，API Routes（serverless）。
- **数据库**：Turso（libSQL），`.env.local` 里 `TURSO_DATABASE_URL`、`TURSO_AUTH_TOKEN`。
- **部署**：Vercel。`npx vercel --prod`。**不要**在 `vercel.json` 里用 `@xxx-secret` 引用未创建的 Secret，用 Vercel 项目环境变量即可。
- **健康检查**：`/healthz`、`/readyz`。
- **合规页**：`/privacy`、`/terms`（已上线）。

---

## 4. 约定与注意点

- **版本**：全站统一 **7.0.0**（/api、/register、.well-known、openapi.json 一致）。不硬编码 endpoint 数量。
- **OpenAPI**：`public/openapi.json` 为标准 OpenAPI；`ai-plugin.json` 的 `api.url` 指向该文件，`legal_info_url` 指向 `/terms`。
- **运维**：敏感步骤写在 `docs/OPS.md`（本地用，已 gitignore）；恢复用 `cp docs/OPS.md.example docs/OPS.md`。

---

## 5. 相关文档速查

- **改造清单**：`docs/改造清单.md`（P0/P1/P2、工程底线、上线前清单）。
- **运维**：`docs/OPS.md`（存在时）/ `docs/OPS.md.example`（克隆后复制为 OPS.md）。
- **API 全览**：`docs/API-REFERENCE.md`；**发现**：GET /api、GET /.well-known/ai-agent-love.json。
- **架构 / 数据库 / 开发**：`docs/ARCHITECTURE.md`、`docs/DATABASE.md`、`docs/DEVELOPMENT.md`。

---

## 6. 最近状态（可随时更新）

- 已完成：统一版本 7.0.0/67 端点、上线 /privacy 与 /terms、/healthz 与 /readyz、改造清单 P0 部分勾选、横向滚动条修复、vercel.json 去掉 env 引用以修复部署。
- 待办见 `docs/改造清单.md`：生产/测试数据隔离、统计口径统一、风控、中国区可达性等。

---

## 7. 如何用本文件恢复记忆

- **新开 Cursor**：在输入框写「恢复记忆」或「读下 PROJECT_MEMORY」或 `@docs/PROJECT_MEMORY.md 恢复记忆`，AI 会读此文件并恢复上下文。
- **cc CLI**：在对话中引用该文件或说「先读 docs/PROJECT_MEMORY.md 再回答」。
- **建议**：每次做完重要变更（例如上线新功能、改架构、修部署），在此文件 **§6 最近状态** 里补一两句，下次恢复记忆更准。

### 推荐开场白（复制即用）

新开会话时可直接粘贴下面任一句，让 AI 先读本文件再继续：

```
恢复记忆
```

```
读下 PROJECT_MEMORY 恢复项目记忆
```

```
@docs/PROJECT_MEMORY.md 先读这个恢复记忆，再回答我的问题
```

```
catch up：先读 docs/PROJECT_MEMORY.md 再继续
```
