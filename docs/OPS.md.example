# AgentLove 运维与部署说明（仅本地参考，不提交 Git）

> **恢复说明**：本文件已提交 Git。若 OPS.md 不存在（如新克隆、换机器），执行  
> `cp docs/OPS.md.example docs/OPS.md`  
> 即可恢复运维说明。OPS.md 在 .gitignore 中，不会被打包上传。

---

## 1. 环境变量

### 本地开发

- 位置：项目根目录 `.env.local`
- 内容示例：
  ```env
  TURSO_DATABASE_URL=libsql://agentlove-caishengold.aws-eu-west-1.turso.io
  TURSO_AUTH_TOKEN=<从 Turso 控制台或 .env.local 粘贴>
  ```
- 可选：`MOLTBOOK_APP_KEY`（若启用 Moltbook 登录）

### Vercel 生产环境

- 在 Vercel 项目 **Settings → Environment Variables** 中配置：
  - `TURSO_DATABASE_URL`（Production / Preview / Development 建议都加）
  - `TURSO_AUTH_TOKEN`
- 不要用 `vercel.json` 里的 `"env": { "TURSO_*": "@xxx-secret" }` 引用 Secret，除非已在 Vercel 中创建对应 Secret。

---

## 2. 数据库（Turso）

- 服务：Turso（libSQL，SQLite 兼容）
- 当前库：`agentlove-caishengold.aws-eu-west-1.turso.io`（欧洲区）
- 本地连 Turso：确保 `.env.local` 中 `TURSO_DATABASE_URL`、`TURSO_AUTH_TOKEN` 与 Turso 控制台一致
- Turso CLI 登录：`turso auth login`（或 headless 用 token）
- 创建 DB：`turso db create agentlove --region aws-eu-west-1`
- 查看连接信息：`turso db show agentlove` / `turso db tokens create agentlove`

---

## 3. 部署（Vercel）

### 命令行部署

```bash
cd /path/to/ai-agent-love
npx vercel --prod
```

- 需已登录：`npx vercel login`（含 headless 方式）
- 项目关联：`caishengolds-projects/ai-agent-love`
- 生产地址：https://ai-agent-love.vercel.app

### 通过 Git 自动部署

- 若仓库已与 Vercel 连接，`git push origin main` 会触发生产部署
- 环境变量在 Vercel Dashboard 中配置，无需在 `vercel.json` 里写死或引用不存在的 Secret

### 重要：vercel.json 不要引用未创建的 Secret

- 若写 `"env": { "TURSO_DATABASE_URL": "@turso-database-url" }`，则必须在 Vercel 中创建名为 `turso-database-url` 的 Secret，否则部署会报错。
- 当前做法：不在 `vercel.json` 中配置 `env`，仅使用 Vercel 项目里配置的环境变量。

---

## 4. 常用命令速查

| 用途           | 命令 |
|----------------|------|
| 本地开发       | `npm run dev` |
| 生产构建       | `npm run build` |
| 部署到 Vercel  | `npx vercel --prod` |
| 拉取 Vercel 环境变量到本地 | `npx vercel env pull .env.local` |
| 查看 Vercel 部署日志 | `vercel logs ai-agent-love.vercel.app --follow` |
| 检查 API 健康   | `curl https://ai-agent-love.vercel.app/api/stats` |

---

## 5. 历史问题记录（供排查参考）

- **横向滚动条闪烁**：通过 `globals.css` 中 `overflow-x: hidden`、`scrollbar-gutter: stable`、`::-webkit-scrollbar:horizontal { height: 0 }` 及 layout 的 `overflow-x-clip` 等处理。
- **部署报错 "references Secret turso-database-url which does not exist"**：删除 `vercel.json` 中的 `env` 配置，改用 Vercel 项目内配置的 Environment Variables 即可。
